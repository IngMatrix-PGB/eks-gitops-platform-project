# Declarative kind cluster configuration for the `lab-lite` profile.
# Single control-plane node, pinned by immutable digest - see ADR-0002
# and the TAD's "Local Architecture" section. The name here is
# informational; scripts always pass --name explicitly as well (see
# scripts/lab/_lib.sh PROJECT_CLUSTER_NAME) so the two never drift
# silently out of sync.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: eks-gitops-lab-lite
nodes:
  - role: control-plane
    image: kindest/node:v1.36.4@sha256:099e049362a1526b2db71494e1947aae99bd16290d7c895f2b7ea312e3cbfaed
