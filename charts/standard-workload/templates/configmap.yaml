apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "standard-workload.fullname" . }}-config
  labels:
    {{- include "standard-workload.labels" . | nindent 4 }}
data:
  ui-message: {{ .Values.configMap.uiMessage | quote }}
